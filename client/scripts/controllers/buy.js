//quantity
//currentPrice
//updateBalance() -->puts currentPrice in avePrice send buy function to chris

myApp.controller('BuyController', ['fleaMarketService', function(fleaMarketService) {
  console.log("BuyController sourced!");

  // var buy = this;
  // console.log(this);
  // buy.currentPrice = setInterval(adjPrice(currentPrice), 15000);
  //
  // var currentPrice = 1.35;
  //
  // var setTimer = setInterval(adjPrice(currentPrice), 15000);
  //
  // function adjPrice(currentPrice){
  //   var max = 0.5;
  //   var min = -0.5;
  //   currentPrice = Math.floor(Math.random() * (max - min + 1)) + min;
  //   console.log(currentPrice);
  //   return currentPrice;
  // }
  //
  // adjPrice(currentPrice);

}]);
